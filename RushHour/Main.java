package RushHour;

import java.awt.Dimension;

// Main class to initialize and run the game
public class Main {
	public static void main(String[] args) {
		// Create a new ScreenManager instance with a window size of 1000x800 pixels
		// This will initialize the game and display the initial screen
		ScreenManager screenManager = new ScreenManager(new Dimension(1000, 800));
	}
}